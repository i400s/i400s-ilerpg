A project to write a set of "shell" ILE sources which will allow the creation of standardised work with programs.

An original monolythic style shell program was used as the basis for this project. 

This branch of the project (master) contains no source, it is just an empty(ish) place holder.